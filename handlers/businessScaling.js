export const BUSINESS_SCALING_COMMAND = "direction_business_scaling";

export async function handleBusinessScalingSelection({ env, userId, peerId, payload }) {
	console.log("Business scaling handler placeholder:", JSON.stringify({ userId, peerId, payload }));
}
